apiVersion: apps/v1
kind: Deployment
metadata:
  name: creto-oversight
  namespace: creto-enablement
  labels:
    app: creto-oversight
    app.kubernetes.io/name: creto-oversight
    app.kubernetes.io/component: oversight
    app.kubernetes.io/part-of: creto-enablement
spec:
  replicas: 3
  selector:
    matchLabels:
      app: creto-oversight
  template:
    metadata:
      labels:
        app: creto-oversight
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: creto-oversight
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        fsGroup: 65532
      containers:
        - name: oversight
          image: ghcr.io/creto-systems/enablement/oversight:latest
          imagePullPolicy: Always
          ports:
            - name: grpc
              containerPort: 50052
              protocol: TCP
            - name: http
              containerPort: 8081
              protocol: TCP
          envFrom:
            - configMapRef:
                name: creto-oversight-config
            - secretRef:
                name: creto-oversight-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: creto-oversight
                topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: creto-oversight
  namespace: creto-enablement
  labels:
    app: creto-oversight
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 50052
      targetPort: grpc
    - name: http
      port: 8081
      targetPort: http
  selector:
    app: creto-oversight
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: creto-oversight
  namespace: creto-enablement
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: creto-oversight-config
  namespace: creto-enablement
data:
  LOG_LEVEL: "info"
  GRPC_PORT: "50052"
  HTTP_PORT: "8081"
  METRICS_ENABLED: "true"
  SLACK_ENABLED: "true"
  EMAIL_ENABLED: "true"
