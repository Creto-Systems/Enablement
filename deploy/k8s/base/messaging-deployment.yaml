apiVersion: apps/v1
kind: Deployment
metadata:
  name: creto-messaging
  namespace: creto-enablement
  labels:
    app: creto-messaging
    app.kubernetes.io/name: creto-messaging
    app.kubernetes.io/component: messaging
    app.kubernetes.io/part-of: creto-enablement
spec:
  replicas: 3
  selector:
    matchLabels:
      app: creto-messaging
  template:
    metadata:
      labels:
        app: creto-messaging
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8083"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: creto-messaging
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        fsGroup: 65532
      containers:
        - name: messaging
          image: ghcr.io/creto-systems/enablement/messaging:latest
          imagePullPolicy: Always
          ports:
            - name: grpc
              containerPort: 50054
              protocol: TCP
            - name: http
              containerPort: 8083
              protocol: TCP
          envFrom:
            - configMapRef:
                name: creto-messaging-config
            - secretRef:
                name: creto-messaging-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: creto-messaging
                topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: creto-messaging
  namespace: creto-enablement
  labels:
    app: creto-messaging
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 50054
      targetPort: grpc
    - name: http
      port: 8083
      targetPort: http
  selector:
    app: creto-messaging
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: creto-messaging
  namespace: creto-enablement
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: creto-messaging-config
  namespace: creto-enablement
data:
  LOG_LEVEL: "info"
  GRPC_PORT: "50054"
  HTTP_PORT: "8083"
  METRICS_ENABLED: "true"
  ENCRYPTION_ALGORITHM: "AES-256-GCM"
